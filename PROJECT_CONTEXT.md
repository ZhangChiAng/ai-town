# AI 小镇双层人格实验系统：项目基线

本文档是当前产品理解、信息边界和实现行为的唯一需求基线。旧的单层人格、
四字段拼接、压缩记忆和个人消息时间线设计均已废止。

## 1. 实验目的与核心假设

这是一个由同一个大模型驱动三个 Agent 的手工、逐步 AI 小镇实验系统。

当前核心假设是：

> 同一个人的内层人格和外层人格拥有彼此独立的上下文。相对稳定、以自然语言
> 写在内层提示词中的欲望与恐惧会使内层对经验形成带偏见的判断，并向外层
> 施压；外层依据自己的现实身份、经验和后果意识接受、反驳或修饰这些判断，
> 最终决定真正对外说什么。

系统要让“外部事件 → 内层判断 → 外层表达”可以逐轮观察、编辑草稿、确认和
回退，但不得用额外的预设心理机制替代模型在自然语言上下文中的判断。

因此明确不引入：

- 欲望值、恐惧值、关系值或其他心理数值；
- 状态机、显式心理模块或自动人格成长；
- 自动回忆、检索、摘要、上下文截断或压缩；
- 自动记忆更新；
- 自动运行的社会模拟；
- 用户直接插入或改写“内心声音”的独立干预机制。

## 2. Agent 与信息边界

一个场景固定包含 Agent A、B、C，三者与两个人格层共用该场景不可变的模型
绑定。新场景必须显式选择当前配置中的一个模型；显式未绑定的 v6 场景只允许
绑定一次，不能换绑。每个 Agent 只保存：

- 固定 `id` 与可编辑 `name`；
- `inner_context`：一份可完整编辑的 `system_prompt` 和全部已确认内层
  turns；
- `outer_context`：另一份可完整编辑的 `system_prompt` 和全部已确认外层
  turns；
- 只属于该 Agent 的 FIFO `pending_events`。

不存在供 Agent 使用的公共时间线、全局 Event Log、全局客观记忆或 AB/AC/BC
分会话。界面的混合历史只是从一个 Agent 自己的事件与两层 turns 派生出的
观察视图，不是额外持久化上下文。

一次模型请求只能包含：

- 当前 Agent 当前人格层的已保存 `system_prompt`；
- 当前人格层的全部已确认 turns；
- 按第 4 节格式构造的本轮 user 输入；
- 调用模型所需的模型名、token 上限与缓存传输元数据。

请求不得包含另一层的 system prompt、另一层的完整历史、其他 Agent 的
上下文、其他 Agent 的待处理事件、未来事件、场景回退栈、显示顺序号、
观察者信息或未确认草稿。固定本轮输入中明确出现的上回合外层话语或本轮内层
输出，是两层之间唯一允许的逐轮信息传递。

用户拥有观察和手工推进全场景的上帝视角，Agent 不知道观察者存在。

## 3. 手工双阶段闭环

系统不自动推进。一个 Agent 必须有待处理事件才能开始新回合，每个完整回合
固定为：

1. 外部事件进入指定 Agent 的 FIFO 队列；
2. 用户为该 Agent 生成内层草稿；
3. 用户可以编辑、放弃或重新生成内层草稿；
4. 用户确认内层，后端消费队首事件并保存内层 turn；
5. 用户生成外层草稿；
6. 用户可以编辑、放弃或重新生成外层草稿；
7. 用户确认外层，后端保存外层 turn，并把它转成接收者的新事件。

每次“生成”或“重新生成”恰好调用模型一次。确认不调用模型。生成不会写
scene JSON，未确认草稿、token usage、调用状态令牌、请求快照和提供商返回的
临时 reasoning 只存在浏览器。刷新页面会丢失这些内容；reasoning 不会进入
确认数据或任何后续请求。

确认后的内层 turn 会形成可持久化的半回合。页面刷新并重新打开场景后，该
Agent 直接恢复到等待外层阶段，不需要重新生成内层。

## 4. 两层的逐字输入格式

内层首轮 user 文本为：

```text
外部事件：
{本轮事件}
```

同一 Agent 已有上一轮外层输出时，内层 user 文本为：

```text
外层人格上一轮对 Agent {接收者 ID}（{接收者当前姓名}）说：
{去掉 To X: 后的上回合外层正文}

外部事件：
{本轮事件}
```

接收者 ID 来自该 Agent 已确认的上一轮外层 turn，姓名来自场景中该接收者
当前的 `name`。外层 turn 仍保存规范化的完整 `To X: 正文`；这里只把它转换成
带有实际说话方向的自然语言，并去掉地址协议前缀后传给内层。

外层本轮 user 文本始终为：

```text
外部事件：
{本轮事件}

你内心有一个声音：
{本轮已确认内层输出}
```

这些标题、空行和正文与实际模型请求逐字同源。前端不重组或装饰它们。

两层历史各自完整发送：每个已确认 turn 的 `input` 映射为 user message，
`output` 映射为 assistant message，然后追加本轮未确认 user 输入。系统不做
自动截断、摘要、检索或记忆替换。

## 5. 输出与事件路由

内层输出是非空自然文本，可以包含多行。内层只能向同一个人的外层表达判断、
感受和冲动，不能在现实中行动或直接向其他 Agent 发消息。

外层输出必须恰好为一行 `To X: 正文`，其中 X 只能是另外两个 Agent 之一，
不能发给自己。确认接口以完整可见文本解析接收者并规范化空格和冒号：

- 发送方外层 turn 保存 `To B: 正文`；
- 接收方队尾新增不可编辑事件 `From A: 正文`；
- 未参与的第三个 Agent 不获得任何信息。

用户可以给一个指定 Agent 创建非空手工事件；手工事件不广播。只有仍在队列
中的手工事件可以编辑或删除。由外层消息生成的 Agent 事件不能直接编辑或
删除。

所有队列严格 FIFO。没有待处理事件时不能生成内层；已有已确认内层半回合时
不能开始下一次内层。

## 6. 持久化、顺序与回退

当前 scene schema 为 v6，不迁移或读取任何旧 schema。每个场景保存显式的
`model: string | null`；新建场景总是绑定具体模型，`null` 只表示尚待一次性
绑定的 v6 场景。

每个内层 turn 保存：

- `call_id`、`event_id` 和全场景显示 `sequence`；
- 实际发送的本轮 `input`；
- 用户最终确认的 `output`；
- 被消费事件的完整快照。

每个外层 turn 保存：

- `call_id`、对应的 `event_id` 和显示 `sequence`；
- 实际发送的本轮 `input`；
- 规范化后的确认 `output`；
- `recipient_id` 与生成的 `generated_event_id`。

事件保存自己的 ID、显示顺序、正文、手工或 Agent 来源，以及 Agent 事件所需
的来源调用引用。事件在待处理队列或一个已确认内层 turn 中只出现一次。

场景级系统元数据只有不可变 `model`、全局 `rollback_stack` 调用引用和单调
递增的 `next_sequence`。后两者只用于界面排序、完整性校验和回退，绝不进入
模型请求。回退后顺序号允许留下空缺，不回收也不影响 FIFO。

“回退最近确认”每次只操作整个场景回退栈顶部：

- 回退内层：删除该内层 turn，把其消费的事件放回原 Agent 队首；
- 回退外层：删除该外层 turn 和它在接收者队列中生成的事件，保留对应内层
  turn，使发送方恢复为等待外层。

如果外层生成的事件已被接收者消费，则接收者内层调用必然位于更高的全局回退
栈位置，必须先逐次回退。无可回退调用返回 `409`。

所有场景写入使用同目录临时文件与原子替换。队列消费、turn 追加、外层路由和
回退都在同一个加锁的 read-modify-write 操作中完成，不允许部分写入。

## 7. 并发与确认规则

生成响应包含当前 `event_id`、浏览器使用的 `call_id`、文本、临时 reasoning、
token usage、实际请求快照和由协议无关业务上下文计算的状态令牌。

`state_token` 哈希场景绑定模型、Agent、人格层、当前完整事件、当前层
`system_prompt`、当前层全部已确认 turns 和本轮 input。它不哈希 adapter
payload；请求字段、缓存传输元数据或 adapter 实现变化不会单独使草稿失效。

确认时后端从当前已保存场景重新构造同层业务上下文，并校验：

- 当前人格阶段；
- 当前事件 ID；
- 协议无关状态令牌；
- 调用 ID 尚未确认；
- 当前层输出格式。

模型绑定、事件、相关 system prompt、同层历史、本轮 input 或阶段在生成后发生
变化时，确认返回 `409`。因此后续内层草稿生成后，上一轮接收者改名也会因为
本轮 input 中的当前姓名变化而使草稿失效。无效的用户编辑外层输出返回 `422`。
失败不改写 JSON，也不会清空浏览器中的草稿。

预览和生成要求场景绑定模型仍在当前进程注册表中，否则返回 `409`。确认不访问
模型注册表，也不调用模型；只要浏览器草稿的业务状态仍然有效，即使其模型已从
当前配置移除，仍可确认。查看、编辑场景与事件以及回退也不依赖模型可用性。
创建场景或一次性绑定时选择当前未配置模型返回 `422`。

## 8. 默认双层提示词

新场景为每个 Agent 创建两份可直接编辑和保存的完整提示词，不提供槽位、
自动拼接或“重新拼接”功能。

默认内层提示词说明：

- 它知道自己是内层，只对外层说话；
- 欲望与恐惧使它形成可能片面、情绪化或矛盾的判断；
- 它像自然出现的内在声音一样向外层施压；
- 它不能行动、不能直接发消息，也不能替外层作最终决定；
- 输出允许多行非空自然文本。

默认外层提示词说明：

- 用户应直接在完整文本中写明现实身份、经历和关系；
- 它听取内层意见但可以接受、反驳、压制或修饰；
- 它负责现实交流并承担后果；
- 它无法读取内层隐藏提示、其他 Agent 上下文或观察者信息；
- 它的自我理解可能不完整或出错；
- 它必须用单行非自己的 `To X: 正文` 输出。

每个 Agent 默认外层提示词直接写入自己的固定 ID 和可选接收 ID。用户后续
可以自由修改完整文本，但保存的两份 system prompt 都不得为空白。

## 9. 模型后端、Prompt cache 与可观测性

进程可按配置顺序注册一个或多个模型，也允许同一协议配置多个模型。场景只绑定
大小写敏感的具体模型名；协议是后端内部 factory key，不出现在模型选择界面、
scene 数据或 model-options API 中。所有后端使用同一份人格层隔离规则和逐字
输入，但具体 adapter 保留自己的原生请求结构。

Anthropic 的内层和外层分别维护 block-level 5 分钟缓存断点：

- 当前层 system prompt 之后一个断点；
- 当前层历史非空时，最后一个已确认 output 之后一个滚动断点；
- 当前未确认 user 输入不缓存。

Responses 使用 `instructions`、完整 `input`、`store=false` 与提供商自动缓存，
不发送 `cache_control` 或其他 Anthropic 元数据。

生成结果展示提供商返回的缓存写入、缓存读取、未缓存输入和输出 token 数。
缓存门槛与是否命中由模型提供商决定，系统不填充无意义文本。

请求预览按 `inner|outer` 明确选择层级，不调用模型、不写 JSON。外层尚无已
确认内层时拒绝预览。响应中的有序 `context` 固定为 system、完整历史的
user/assistant 交替项和当前 user；可读视图只使用这份中性上下文。原始
`request` JSON 保留任意 adapter payload，前端不解析供应商字段。界面不能生成
模型未见的姓名、方向或解释文本，也不能隐藏模型已见的正文。

两种协议统一返回缓存写入、缓存读取、未缓存输入和输出 token 数，并只提取
提供商允许公开的 thinking、reasoning summary 或 reasoning text。签名、加密
内容和 redacted thinking 不展示；所有 reasoning 都是未持久化的观察数据。

## 10. 技术与数据边界

- 后端：Python + FastAPI；
- 前端：Vue 3 + TypeScript + Vite；
- 持久化：`data/scenes/<scene-id>.json`；
- 模型调用：Anthropic Messages 与 OpenAI Responses 两种兼容端点；
- 一个场景的三个 Agent、两个层级始终共用该场景绑定的同一模型；
- Linux 开发，并可在安装 Python、uv、Node.js 与 npm 的 Windows 机器上
  本地运行。

仓库根目录必须有被 Git 忽略的 `models.toml`。它包含一个或多个有序
`[[models]]`，每项严格只有：

- `model`：大小写敏感且全局唯一的场景绑定标识；
- `protocol`：只供注册表查找 factory 的内部 key；
- `base_url`：有效的绝对 HTTP(S) URL；
- `api_key_env`：按模型粒度保存该模型真实密钥的环境变量名，而非密钥本身。

当前正式 protocol key 为 `anthropic_messages` 和 `openai_responses`。
配置层不硬编码协议枚举；注册表拒绝没有已注册 factory 的 key。TOML 声明顺序
就是后端创建顺序和界面模型顺序，`GET /api/model-options` 的每个元素严格只有
`{model}`。scene schema 保持 v6，并继续只用 `scene.model` 保存绑定。

进程环境中的密钥优先于可选 `.env`；真实密钥不得写入 TOML。仓库提交
`models.example.toml` 作为结构示例。旧的六变量配置方式已彻底停止解析，没有
兼容回退。空清单、重复 model、未知字段、非法 URL、非法密钥变量名、缺失密钥
或未知协议都使启动失败，错误不得包含配置值或 secret。

注册表按 TOML 顺序创建 backend。部分创建失败时，已创建项逆序关闭；正常退出
时所有 backend 也逆序关闭。新增正式协议只需实现 adapter、注册 factory 并通过
共享 contract tests，不修改业务 workflow、路由或前端。

`data/` 是被 Git 忽略的纯运行时目录，不包含产品基线或参考材料。应用按需创建
`data/scenes/`，只在其中保存 schema v6 场景 JSON。

## 11. 明确非目标

- 不保留单层对照模式、兼容开关或应用内盲测；
- 不提供独立公共时间线；
- 不允许手工事件广播；
- 不实现自动模拟、自动记忆、自动回忆、人格成长或心理数值；
- 不做上下文压缩；
- 不在服务器持久化未确认草稿；
- 不自动解决未来尚未讨论的世界行为或心理机制。
