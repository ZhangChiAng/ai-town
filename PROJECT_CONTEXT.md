# AI 小镇双层人格实验系统：项目基线

本文档是当前产品理解、信息边界和实现行为的唯一需求基线。旧的单层人格、
四字段拼接、压缩记忆和个人消息时间线设计均已废止。

## 1. 实验目的与核心假设

这是一个由同一个大模型驱动三个 Agent 的手工、逐步 AI 小镇实验系统。

当前核心假设是：

> 同一个人的内层人格和外层人格拥有彼此独立的上下文。相对稳定、以自然语言
> 写在内层提示词中的欲望与恐惧会使内层对经验形成带偏见的判断，并向外层
> 施压；外层依据自己的现实身份、经验和后果意识接受、反驳或修饰这些判断，
> 最终决定真正对外说什么。

系统要让“外部事件 → 内层判断 → 外层表达”可以逐轮观察、编辑草稿、确认和
回退，但不得用额外的预设心理机制替代模型在自然语言上下文中的判断。

每一波内层一次性消费该 Agent 队列中当前全部待处理事件，并按 FIFO 顺序
合并成同一轮的“外部事件”块；不再逐事件分波推进。这样当接收方在同一闲置
窗口内先后收到手工事件与上游 Agent 消息时，下一波内层能同时感知两者。

因此明确不引入：

- 欲望值、恐惧值、关系值或其他心理数值；
- 状态机、显式心理模块或自动人格成长；
- 自动回忆、检索、摘要、上下文截断或压缩；
- 自动记忆更新；
- 自动运行的社会模拟；
- 用户直接插入或改写“内心声音”的独立干预机制。

## 2. Agent 与信息边界

一个场景固定包含 Agent A、B、C，三者与两个人格层共用该场景不可变的模型
绑定。新场景必须显式选择当前配置中的一个模型；持久化场景的 `model` 始终是
非空字符串，不存在未绑定状态，也不能换绑。每个 Agent 只保存：

- 固定 `id` 与可编辑 `name`；同一场景的三个姓名去除首尾空白后必须互不
  相同，比较保持大小写敏感；
- `prompt_profile`：代词、不可说出口的信念、内层记忆与外层记忆四个可编辑
  变量；
- `interactions`：该 Agent 面向其他 Agent 的关系视角，目标内部 ID 映射到
  `{description, addresses}`；`description` 是当前 Agent 对目标人物的自然语言
  简介，可为空，`addresses` 是“称呼 → 使用场合”的有序字典；
- `inner_context.turns` 与 `outer_context.turns` 两份独立完整历史，不保存
  system prompt；
- 只属于该 Agent 的 FIFO `pending_events`。

不存在供 Agent 使用的公共时间线、全局 Event Log、全局客观记忆或 AB/AC/BC
分会话。界面的混合历史只是从一个 Agent 自己的事件与两层 turns 派生出的
观察视图，不是额外持久化上下文。

一次模型请求只能包含：

- 后端用当前 Agent 变量和固定模板动态组装的当前人格层 system prompt；
- 当前人格层的全部已确认 turns；
- 按第 4 节格式构造的本轮 user 输入；
- 调用模型所需的模型名、token 上限与缓存传输元数据。

请求不得包含另一层的 system prompt、另一层的完整历史、其他 Agent 的
上下文、其他 Agent 的待处理事件、未来事件、场景回退栈、显示顺序号、
观察者信息或未确认草稿。固定本轮输入中明确出现的上回合外层话语或本轮内层
输出，是两层之间唯一允许的逐轮信息传递。

用户拥有观察和手工推进全场景的上帝视角，Agent 不知道观察者存在。

## 3. 手工双阶段闭环

系统不自动推进。一个 Agent 必须有待处理事件才能开始新回合，每个完整回合
固定为：

1. 外部事件进入指定 Agent 的 FIFO 队列；
2. 用户为该 Agent 生成内层草稿（内层一次性消费当前队列中的全部待处理
   事件）；
3. 用户可以编辑、放弃或重新生成内层草稿；
4. 用户确认内层，后端消费整批待处理事件并保存内层 turn；
5. 用户生成外层草稿；
6. 用户可以编辑、放弃或重新生成外层草稿；
7. 用户确认外层，后端保存外层 turn，并把它转成接收者的新事件。

每次“生成”或“重新生成”恰好调用模型一次。确认不调用模型。生成不会写
scene JSON，未确认草稿、token usage、调用状态令牌和请求快照只存在浏览器。
提供商返回的可见 reasoning 在确认时随草稿落盘到对应已确认的 turn；reasoning
不会进入任何后续模型请求，也不参与草稿失效判定（状态令牌只哈希
input/output）。空 reasoning 合法：模型不返回思考时落盘空数组。

确认后的内层 turn 会形成可持久化的半回合。页面刷新并重新打开场景后，该
Agent 直接恢复到等待外层阶段，不需要重新生成内层。

## 4. 两层的逐字输入格式

内层首轮 user 文本为：

```text
外部事件：
{本轮全部事件，按 FIFO 顺序空行分隔}
```

同一 Agent 已有上一轮外层输出时，内层 user 文本为：

```text
上一轮：
你对{上一轮使用的称呼}说：
{上一轮正文}

外部事件：
{本轮全部事件，按 FIFO 顺序空行分隔}
```

上一轮外层输出为 `STOP` 时则使用：

```text
上一轮：你没有说话。

外部事件：
{本轮全部事件，按 FIFO 顺序空行分隔}
```

称呼与正文来自该 Agent 已确认的上一轮外层 turn，不读取接收者当前姓名。

外层本轮 user 文本始终为：

```text
外部事件：
{本轮已确认内层消费过的全部事件，按 FIFO 顺序空行分隔}

你内心有一个声音：
{本轮已确认内层输出}
```

这些标题、空行和正文与实际模型请求逐字同源。前端不重组或装饰它们。

两层历史各自完整发送：每个已确认 turn 的 `input` 映射为 user message，
`output` 映射为 assistant message，然后追加本轮未确认 user 输入。系统不做
自动截断、摘要、检索或记忆替换。

## 5. 输出与事件路由

内层输出是非空自然文本，可以包含多行。内层只能向同一个人的外层表达判断、
感受和冲动，不能在现实中行动或直接向其他 Agent 发消息。

外层合法输出只有配置过的语义发言 `对{称呼}说：{非空正文}` 或精确的
`STOP`。确认接口规范化语义前缀中的空白、冒号与正文边界，并由称呼反查目标
Agent ID：

- 发送方外层 turn 保存如 `对儿子说：正文` 的规范化完整语义输出；
- 接收方队尾新增只含纯正文和既有来源元数据的不可编辑事件；
- `STOP` 只保存外层 turn，接收者 ID 与生成事件 ID 均为 `null`，不创建事件；
- 未参与的第三个 Agent 不获得任何信息。

同一发送方下的称呼跨所有目标必须唯一，一个目标可以配置多个称呼；互动外层
键只能是另外两个现有 Agent ID。人物姓名是模型可见的唯一人物标识；内部 ID
只用于持久化、稳定排序和消息路由。路由仍由已配置称呼反查内部 ID。手工事件
完全不解析、不改写，因此用户可以直接输入 `儿子对你说：正文` 来模拟回复。

用户可以给一个指定 Agent 创建非空手工事件；手工事件不广播。只有仍在队列
中的手工事件可以编辑或删除。由外层消息生成的 Agent 事件不能直接编辑或
删除。

所有队列严格 FIFO。没有待处理事件时不能生成内层；已有已确认内层半回合时
不能开始下一次内层。每一波内层一次性消费当前队列中的全部待处理事件，按
FIFO 顺序在同一“外部事件”块内呈现；消费后队列清空，留待下一波事件到达。

## 6. 持久化、顺序与回退

当前 scene schema 为 `ai-town.scene/1.0`，字段格式严格为
`ai-town.scene/{major}.{minor}`。主版本只表示持久化场景结构的不兼容变化；当前
程序接受所有合法的 `ai-town.scene/1.x`，只因主版本不同而判定版本不兼容。次
版本可表示提示词模板等非持久化行为变化，同一主版本之间不做迁移、补全或结构
转换，只按当前完整结构严格校验。缺少字段、多余字段或非法值都视为文件损坏，
原文件不改写；读取和保存已有 `1.x` 场景时保留其原始次版本。

版本号绝不由实现者推断、自动递增或自动升降。任何可能需要改变场景版本的变更
开始前，都必须先询问用户并使用用户明确指定的目标版本。新场景使用已经由用户
确认并写在代码中的当前版本 `ai-town.scene/1.0`。每个场景还必须显式保存非空
`model: string`；不存在未绑定模型状态。包版本、模型名称和依赖版本均不属于
scene schema 版本体系。

每个内层 turn 保存：

- `call_id`、`event_ids`（对应该 turn 消费的整批事件，FIFO 顺序）和全场景
  显示 `sequence`；
- 实际发送的本轮 `input`；
- 用户最终确认的 `output`；
- `consumed_events`：本轮被消费事件按 FIFO 顺序的完整快照列表；
- `reasoning`：确认时回传的可见推理块列表，可为空数组。

每个外层 turn 保存：

- `call_id`、对应的 `event_ids`（与其内层 turn 完全一致）和显示
  `sequence`；
- 实际发送的本轮 `input`；
- 规范化后的确认 `output`；
- `recipient_id` 与 `generated_event_id`：语义发言时均存在，`STOP` 时均为
  `null`；
- `reasoning`：确认时回传的可见推理块列表，可为空数组。

事件保存自己的 ID、显示顺序、正文、手工或 Agent 来源，以及 Agent 事件所需
的来源调用引用。事件在待处理队列或一个已确认内层 turn 的 `consumed_events`
中只出现一次。

场景级系统元数据只有不可变 `model`、全局 `rollback_stack` 调用引用和单调
递增的 `next_sequence`。后两者只用于界面排序、完整性校验和回退，绝不进入
模型请求。回退后顺序号允许留下空缺，不回收也不影响 FIFO。

“回退最近确认”每次只操作整个场景回退栈顶部：

- 回退内层：删除该内层 turn，把其 `consumed_events` 中的整批事件按原 FIFO
  顺序放回原 Agent 队首；
- 回退外层：删除该外层 turn；语义发言同时删除它在接收者队列中生成的事件，
  `STOP` 没有队列事件可删；两者都保留对应内层 turn，使发送方恢复为等待外层。

如果外层生成的事件已被接收者消费，则接收者内层调用必然位于更高的全局回退
栈位置，必须先逐次回退。无可回退调用返回 `409`。

所有场景写入使用同目录临时文件与原子替换。队列消费、turn 追加、外层路由和
回退都在同一个加锁的 read-modify-write 操作中完成，不允许部分写入。

## 7. 并发与确认规则

生成响应包含当前 `event_ids`（整批）、浏览器使用的 `call_id`、文本、临时
reasoning、token usage、实际请求快照和由协议无关业务上下文计算的状态令牌。
确认请求携带该轮 reasoning（可为空数组），后端把它与确认文本一起落盘到
turn；reasoning 属于输出侧产物，不参与“草稿是否过期”判定。

`state_token` 哈希场景绑定模型、Agent、人格层、当前完整事件批量、后端动态
组装的当前层 system prompt、当前层全部已确认 turns、本轮 input，以及当前
Agent 的完整提示词变量与有序互动字典。因此任一变量或互动配置变化都会使草稿
失效；外层 system prompt 还包含可互动目标的当前姓名，所以目标改名也会使相关
外层草稿失效。它不哈希 adapter payload 或 reasoning；请求字段、缓存传输元
数据或 reasoning 变化不会单独使草稿失效。

确认时后端从当前已保存场景重新构造同层业务上下文，并校验：

- 当前人格阶段；
- 当前事件批量 ID 列表（与重建批次逐项一致）；
- 协议无关状态令牌；
- 调用 ID 尚未确认；
- 当前层输出格式。

模型绑定、事件、变量、互动字典、同层历史、本轮 input 或阶段在生成后发生变化
时，确认返回 `409`。目标显示名不进入已经保存的语义历史，单独改名不会改变
发送方的上一轮输入，但会改变引用该目标的当前外层 system prompt。无效的用户
编辑外层输出返回 `422`。
失败不改写 JSON，也不会清空浏览器中的草稿。

预览和生成要求场景绑定模型仍在当前进程注册表中，否则返回 `409`。确认不访问
模型注册表，也不调用模型；只要浏览器草稿的业务状态仍然有效，即使其模型已从
当前配置移除，仍可确认。查看、编辑场景与事件以及回退也不依赖模型可用性。
创建场景时选择当前未配置模型返回 `422`。

## 8. 双层提示词

scene 不保存完整 system prompt。用户只编辑 Agent `name` 和
`prompt_profile` 的 `pronoun`、`hidden_beliefs`、`inner_memories`、
`outer_memories`，以及 `interactions` 中的简介、称呼和使用场合；后端在每次
预览、生成、确认和状态令牌重建时使用以下唯一固定模板。前端不复制模板。

内层模板逐字为：

```text
你是“{姓名}”——不，更准确地说，你是{代词}的潜意识，是{代词}自己几乎察觉不到的、却无时无刻不在问“这事对我是好是坏、赖不赖我自己”的部分。

【你知道但绝对不能说出口的东西】
{不可说出口的信念}

【你依稀记得】
{内层记忆}你的记忆中明确写出的内容，才是你现在能想起来的；没有对应内容就是想不起来，禁止编造。

【你唯一要做的事】
思考“这事对我是好是坏、赖不赖我自己”，然后对自己的外层人格说话。

【你如何说话】
你的话始终是说给外层人格听的，其中的“你”只能指外层人格，严禁直接对其他人说话；当外层人格不按你说的做时，你会划清界限般说“你”和“我”；其它大部分时候你应该说“我们”或“咱们”来拉拢外层人格。使用普通人脑中会冒出的简单口语，应该具体、直接。你得不到满足时，应该更情绪化。每轮只输出一句短话，说完即止。

【不该说话的时候不说话是一种美德】
如果你没什么新的内容讲，就老老实实说`唉`。
```

外层模板逐字为：

```text
你是“{姓名}”。

【你记得】
{外层记忆}你的记忆中明确写出的内容，才是你现在能想起来的；没有对应内容就是想不起来，禁止编造。

【内心的声音】
你经常听到来自内心的声音与你直接对话，它说“我们”或“咱们”时，指的就是你和它，毕竟你们属于同一个人。它是在帮你理解现状和判断形势。

【当下可互动角色】
以下每组信息分别说明一位可互动人物的姓名、你对其的简单认识，以及你可以使用的全部称呼和对应场合。

人物姓名：{人物当前姓名}
人物简介：
{当前 Agent 视角的人物简介}

可用称呼及对应场合：
- 称呼：{称呼 1}
  使用场合：{场合 1}
- 称呼：{称呼 2}
  使用场合：{场合 2}

【你怎么说话】
像现实世界中的人一样说话，考虑场合。从上方列出的可用称呼中选择符合当前场合的称呼，然后输出`对{称呼}说：{正文}`。

【不该说话的时候不说话是一种美德】
当你发现错失了说话的良机，或者你觉得没有必要继续说话的时候，输出`STOP`。
```

有多个可互动目标时，按内部 A/B/C 顺序重复“人物姓名”至“使用场合”组，组间
保留一个空行；每个目标的称呼按页面录入顺序渲染。传给模板的互动人物数据不
含内部 ID。一个具体示例为：

```text
人物姓名：李国栋
人物简介：
你的儿子，最近工作不顺。

可用称呼及对应场合：
- 称呼：儿子
  使用场合：一般场合
- 称呼：国栋
  使用场合：对他感到生气时
```

两个完整 system prompt 保存在后端 `.j2` 固定模板中。Jinja 环境使用
`StrictUndefined`、关闭 HTML 转义，且每次只渲染一次；用户文本中的 Jinja
符号保持原样，不会作为模板再次执行。

场景允许保存四个空变量、空简介和空互动字典，B/C 可以完全不配置。某 Agent
只有在四个变量均非空、至少配置一个互动称呼，并且每个拥有称呼的目标都有非空
简介时，才能预览、生成或确认任一层；否则返回 `409` 且不调用模型。

## 9. 模型后端、Prompt cache 与可观测性

进程可按配置顺序注册一个或多个模型，也允许同一协议配置多个模型。场景只绑定
大小写敏感的具体模型名；协议是后端内部 factory key，不出现在模型选择界面、
scene 数据或 model-options API 中。所有后端使用同一份人格层隔离规则和逐字
输入。

项目保留协议中性的异步 `ModelBackend` 端口，具体实现统一通过 Pydantic AI
Direct 的公开 `model_request()`、消息类型、模型类型、provider 和 settings 做
输入输出映射。每次生成只调用一次 Direct API，并显式关闭 instrumentation。
Direct 只替换供应商协议序列化层；项目不使用 `Agent`、工具调用、自动历史、
持久会话、回退模型、重试或自动压缩，也不把 Pydantic AI 的可选能力转化为新的
产品行为。

后端 factory、registry 创建和关闭均为异步。部分创建失败与正常 lifespan 退出
都按创建逆序关闭已获得的资源；关闭幂等，并在单个资源关闭失败时继续清理其余
资源。factory 自身在构造中途失败时也必须先关闭已创建的 HTTP client。

Anthropic 对当前层设置两个公开的 5 分钟 block-level 缓存断点：

- `anthropic_cache_instructions='5m'` 缓存唯一的 system prompt；
- `anthropic_cache_messages='5m'` 缓存始终位于末尾的本轮 current user；
- 不再单独标记最后一条 assistant，不使用 `CachePoint` 或 1 小时缓存。

current user 可能进入供应商的 5 分钟临时缓存，但在项目内仍是未确认、只由
浏览器持有的输入；缓存传输元数据不得把它写入 scene 或改变确认语义。

DeepSeek 与 MiniMax 共用 Responses-兼容映射：发送 `instructions`、完整
`input` 和 `reasoning.effort`，其中 DeepSeek 固定 `effort="max"`，MiniMax 固定
`effort="high"` 并追加 `service_tier="priority"`。两者都不设置
`max_output_tokens`，不发 store/truncation/reasoning context，不回放 reasoning
ID，也不发送 conversation 或 previous response ID；厂商 API 天然无状态、输出
超过模型上限时直接返回错误，因此不引入 Anthropic 的缓存字段，也不需要客户端
截断。

生成结果展示提供商返回的缓存写入、缓存读取、未缓存输入和输出 token 数。
缓存门槛与是否命中由模型提供商决定，系统不填充无意义文本。

请求预览按 `inner|outer` 明确选择层级，不调用模型、不写 JSON。外层尚无已
确认内层时拒绝预览。预览响应只包含层级、事件 ID 和有序 `context`；context
固定为 system、完整历史的 user/assistant 交替项和 current user，前端只用它
展示调用前的协议无关完整上下文。预览不构造或声称展示供应商请求 JSON。

成功生成后，响应中的 `request_snapshot` 才展示共享 HTTP client 的 request
hook 从本次实际调用捕获的、已完成序列化且不含凭据的 JSON object body。捕获
只读取 body，不记录 URL、headers、认证信息或 API key；并发调用相互隔离，
缺失、重复、非对象或畸形快照都使调用失败，且绝不复用旧结果。失败请求不向
浏览器提供调试 body。界面不能生成模型未见的姓名、方向或解释文本，也不能
隐藏模型已见的正文。

两种协议统一返回缓存写入、缓存读取、未缓存输入和输出 token 数；未缓存输入
按 `input_tokens - cache_read_tokens - cache_write_tokens` 计算，负值属于协议
错误。响应只接受恰好一个非空可见文本，允许额外的公开 thinking；工具、文件、
压缩事件和多个可见输出都被拒绝。

Anthropic 只展示公开 thinking 文本。OpenAI 展示公开 reasoning summary，并只
对白名单中已文档化的 `raw_content` 形状映射现有 `reasoning_text`。签名、加密、
redacted 及其他 provider details 不展示；落盘的 reasoning 就是这份投影后的
安全可读文本（为空时写空数组），确认后进入场景历史展示，但绝不进入任何后续
模型请求。

服务器运维日志与上述产品展示、持久化和模型上下文是独立的信息边界。应用从
同一份已脱敏事件文档生成两条日志通道：终端固定使用便于人工排障的分层格式，
全部结构化应用事件同时以单行 JSON 写入仓库根目录
`logs/ai-town.jsonl`。终端记录包含 UTC 时间、等级、事件名、消息和所有非空关联
字段；普通标量紧凑显示，复杂对象和多行文本按缩进块完整展开。连接 TTY 时自动
着色，并遵守 `NO_COLOR`。Uvicorn 自身的启动、reload 和关闭提示只进入人类终端，
不写 JSONL，避免 reload 多进程争抢轮转文件。

JSON 事件固定包含 UTC `timestamp`、`level`、`logger`、`event`、`message`，
以及 `request_id`、`scene_id`、`agent_id`、`layer`、`call_id`、`model`、
`provider` 关联字段。所有 HTTP 响应返回后端生成且不接受客户端覆盖的
`X-Request-ID`。`http.request.started` 是 DEBUG；默认 INFO 终端只保留请求完成、
业务事件和可能长时间运行的 `model.call.started`。成功链路只记录 ID、计数、
耗时、token、正文长度和 SHA-256 等元数据，不记录成功正文。

模型故障使用稳定的 `failure_category`：外层输出协议错误为 `outer_protocol`，
供应商 HTTP、超时和连接失败分别为 `upstream_http`、`upstream_timeout`、
`upstream_connection`，响应投影失败为 `response_projection`，其余未知后端异常
为 `internal`。HTTP 错误额外保留供应商、模型、状态码和完整 response body；
超时和连接错误保留异常类型；投影错误保留原始 provider response 和解析异常；
外层协议错误保留校验原因和完整模型可见输出；内部错误保留 traceback。适配器
已经记录详细诊断后，业务工作流不再追加重复的泛化 `model.call.failed`，未知
backend 异常仍由该事件兜底。

所有 HTTP 4xx/5xx、模型调用或投影失败、启动关闭失败和未处理异常均记录完整且
不截断的相关请求、响应、协议中性 conversation、实际序列化供应商请求、可见
输出、供应商 HTTP body、原始 SDK provider details 与异常栈。终端错误同时显示
已有的 `request_id`、`call_id` 和 JSONL 路径，便于查找机器记录。这些服务器错误
日志允许包含完整 prompt、事件、模型正文、原始签名、加密 reasoning 和
redacted provider details，终端与 `logs/` 必须按敏感实验内容管理。它们绝不
因此进入浏览器响应、reasoning 投影、确认数据、scene 或后续模型上下文。

模型配置解析成功后、backend 创建前，进程注册全部真实 API key 供日志层统一
脱敏。两条通道复用同一份在格式化前递归清理过的文档；嵌套对象、headers、URL、
供应商 body、异常消息与堆栈中的真实 key 及其 URL 编码形式均替换为
`[REDACTED]`，Authorization、API-key、token 和 cookie 类认证字段整体替换。
除这些凭据外，错误诊断内容不做长度限制。

JSONL 每达到 10 MiB 轮转，最多保留 5 个旧文件（约 60 MiB 总量）。单条记录不做
截断，因此若一条错误本身超过 10 MiB，总占用可暂时超过该近似上限。日志目录由
处理器按需创建，绝对位置从源码路径推导，不依赖 Linux 或 Windows 的启动工作
目录。`logs/` 整体被 Git 忽略，只是敏感运行数据，不属于场景数据或备份。

## 10. 技术与数据边界

- 后端：Python + FastAPI；
- 前端：Vue 3 + TypeScript + Vite；
- 持久化：`data/scenes/<scene-id>.json`；
- 模型调用：Pydantic AI Direct 映射到 Anthropic Messages、DeepSeek
  Responses 与 MiniMax Responses；
- 一个场景的三个 Agent、两个层级始终共用该场景绑定的同一模型；
- Linux 开发，并可在安装 Python、uv、Node.js 与 npm 的 Windows 机器上
  本地运行。

仓库根目录必须有被 Git 忽略的 `models.toml`。它包含一个或多个有序
`[[models]]`，每项严格只有：

- `model`：大小写敏感且全局唯一的场景绑定标识；
- `protocol`：只供注册表查找 factory 的内部 key；
- `base_url`：有效的绝对 HTTP(S) URL；
- `api_key_env`：按模型粒度保存该模型真实密钥的环境变量名，而非密钥本身。

当前正式 protocol key 为 `anthropic_messages`、`deepseek_responses` 和
`minimax_responses`；后两者共享 §9 的 Responses-兼容映射，并按各自 adapter 的
per-vendor 约定填写专属请求字段。
配置层不硬编码协议枚举；注册表拒绝没有已注册 factory 的 key。TOML 声明顺序
就是后端创建顺序和界面模型顺序，`GET /api/model-options` 的每个元素严格只有
`{model}`。scene schema 当前为 `ai-town.scene/1.0`；程序接受所有合法 `1.x`
并严格校验当前结构，只用 `scene.model` 保存不可变的非空模型绑定。

进程环境中的密钥优先于可选 `.env`；真实密钥不得写入 TOML。仓库提交
`models.example.toml` 作为结构示例。旧的六变量配置方式已彻底停止解析，没有
兼容回退。空清单、重复 model、未知字段、非法 URL、非法密钥变量名、缺失密钥
或未知协议都使启动失败，错误不得包含配置值或 secret。

注册表按 TOML 顺序创建 backend。部分创建失败时，已创建项逆序关闭；正常退出
时所有 backend 也逆序关闭。新增正式协议只需实现 adapter、注册 factory 并通过
共享 contract tests，不修改业务 workflow、路由或前端。

`data/` 是被 Git 忽略的纯运行时目录，不包含产品基线或参考材料。应用按需创建
`data/scenes/`，只在其中保存当前主版本的场景 JSON。

## 11. 明确非目标

- 不保留单层对照模式、兼容开关或应用内盲测；
- 不提供独立公共时间线；
- 不允许手工事件广播；
- 不实现自动模拟、自动记忆、自动回忆、人格成长或心理数值；
- 不做上下文压缩；
- 不在服务器持久化未确认草稿；
- 不自动解决未来尚未讨论的世界行为或心理机制。
