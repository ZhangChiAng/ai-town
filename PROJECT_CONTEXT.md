# AI 小镇最小实验系统：项目基线

本文档记录当前已经确认的产品理解与边界，是后续设计和实现的唯一需求基线。它只细化到足以开始下一阶段工作，不提前补全尚未讨论的系统设计。

## 1. 实验目的与核心假设

这是一个由大模型驱动的三智能体“AI 小镇”最小实验系统。

核心假设是：

> 经验不直接塑造性格。相对稳定、以自然语言写成的“欲望与恐惧”决定 Agent 如何解释经验；解释形成记忆，记忆再影响未来行为。

系统需要让这条链路可以被观察和人工干预，但不能用额外的预设心理机制替代它。

因此，第一版明确不引入：

- 欲望值、恐惧值或其他心理数值；
- 关系值、好感度等关系数值；
- 状态机；
- 显式心理模块；
- 自动回忆或检索式记忆机制；
- 自动运行的社会模拟；
- 自动记忆更新。

## 2. Agent 与信息边界

一个场景包含三个 Agent，三个 Agent 共用同一个模型。每个 Agent 分别拥有：

- 人设；
- 文字化的欲望与恐惧；
- 当前压缩记忆；
- 独立的个人时间线。

个人时间线是该 Agent 所有已确认收发消息按时间排列后的单一序列。系统不是 AB、AC、BC 分会话，也不存在供 Agent 使用的全局 Event Log 或全局客观记忆。

用户还可以向当前选中的单个 Agent 人工写入一段“内心的声音”。它是带明确
来源标记的主观提示，只持久化在目标 Agent 的私人时间线中；其他 Agent 不可见。
它不冒充任何 Agent，不引入观察者角色，也不是自动产生的心理状态或回忆。

消息确认后：

- 发送者的时间线记录“发送给 B：……”；
- 接收者的时间线记录“A：……”；
- 未参与消息的第三个 Agent 不获得任何信息。

因此，同一事件可以被不同 Agent 看到不同部分，并被他们解释成不同的记忆。用户拥有上帝视角，但 Agent 不知道观察者存在。

## 3. 单步交互闭环

系统不自动推进。每次只有用户点击某个 Agent 时，该 Agent 才进行一次模型推理。

一次交互的目标流程是：

1. 用户选择一个 Agent 并触发推理；
2. 模型返回结构化消息草稿，例如 `{"recipient":"B","content":"..."}`；
3. 用户可以编辑草稿或要求重新生成；
4. 用户确认发送；
5. 系统把消息分别写入发送者和接收者的个人时间线；
6. 当前数据保存到 JSON；
7. 应用重启后可以从保存的数据继续。

只有确认后的消息进入双方时间线。必要时，用户可以插入“（系统提问：……）”，要求 Agent 对其理解或行为作出解释。

写入“内心的声音”本身不会触发模型推理或自动发送消息。用户仍需主动点击
“生成草稿”推进一次模型回合。内心声音按时间线原顺序持续进入目标 Agent
后续上下文，直到旧阶段被压缩排除，或用户在它是该 Agent 时间线绝对末条时
将其删除。该栈顶规则也适用于双方消息：内心声音位于消息之后时，不允许越过
它删除更早的消息。

## 4. 记忆与阶段

记忆绝不自动更新。只有用户主动点击“更新记忆”时，系统才调用模型生成新的压缩记忆；更新结果在正式采用前可以由用户编辑。

采用新压缩记忆后：

- 旧阶段只供用户查看；
- 旧阶段聊天不再进入后续模型上下文；
- 后续模型上下文只包含该 Agent 的人设、欲望与恐惧、新压缩记忆，以及新阶段产生的聊天。

人设、欲望、恐惧和压缩记忆都允许用户人工修改。

## 5. 第一版完成闭环

第一版只需要完成以下端到端闭环：

> 创建场景 → 编辑三个 Agent → 选择 Agent 步进 → 调用模型 → 编辑或重生成草稿 → 确认发送 → 写入双方时间线 → JSON 保存 → 重启后继续

用户可在选择 Agent 后、生成草稿前，选择性地写入仅该 Agent 可见的内心声音；
这是一条人工干预支线，不改变上述手动步进闭环。

这个闭环之外的完整世界模拟、复杂心理建模和自动化行为不属于当前目标。

## 6. 技术与运行约束

- 后端：Python + FastAPI；
- 前端：Vue 3 + TypeScript + Vite；
- 持久化：JSON；
- 模型接口：OpenAI Compatible API；
- 三个 Agent 使用同一个模型配置；
- 在 Linux 环境开发；
- 在安装了 Python 和 Node.js 的 Windows 机器上本地运行。

本阶段唯一明确保留的实现决策是：JSON 最终使用单文件还是多文件。业务逻辑不得依赖该选择，具体形式需要封装在存储层内。

本基线不预先定义 API、TypeScript 类型、Python 数据模型或 JSON Schema；这些属于后续实现设计。

## 7. `data/` 的用途

`data/` 是项目本地持久化数据的统一存放目录。使用中产生的场景状态将保存于
此处；具体采用单文件还是多文件 JSON，仍由存储层封装，不改变第 6 节保留的
实现边界。

目录中现有的 Markdown 文件来自此前的人工模拟，用于帮助理解实验意图，特别是：

- 每个 Agent 只依据自己知道的消息行动；
- 同一事件会形成不同的主观印象和压缩记忆；
- 欲望与恐惧通过解释经验影响后续选择；
- 记忆压缩后，下一阶段不再携带旧阶段的完整聊天。

在项目数据格式确定后，这些人工模拟记录会被转换为该格式。在完成转换前，
它们仍只是行为参考，不是正式需求或默认场景，也不应被应用当作可直接加载的
持久化数据。

## 8. 后续设计原则

- 只细化到当前阶段能够开工；
- 不把未决定的事项写成既定事实；
- 不从样例或常见游戏架构中擅自增加先验机制；
- 重大产品或行为设计在实施前先向用户澄清。
